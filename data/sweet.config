# sweet configuration file format
dialect = ansi

format {
	quote   = false
	indent  = tab
	newline = lf

	comma = prepend

	compact = no-spaces-arround
	upperize = keyword identifier
}

#lint {
#	no-star = off
#
#	avoid-name {
#		level   = warning
#		exclude = foo bar
#	}
#
#	columns-count {
#		level   = warning
#		context = *
#		context = view
#		context = cte
#	}
#}