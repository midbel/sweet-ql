# sweet configuration file format
dialect = ansi

format {
	quote   = false
	indent  {
		char = space
		size = 4
	}
	newline = lf

	comma = prepend

	compact = no-spaces-arround
	upperize = none
}

lint {
	avoid-name {
		level   = warning
	}

	columns-count {
		level   = warning
	}
}